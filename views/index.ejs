<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Carnet d'Adresse des amis de Poudlard</title>
</head>
<body>
  <button id='update'>Mise à jour du accio</button>
  <form action="/adresse" method="POST">
      <input type="text" placeholder="nom" name="nom">
      <input type="text" placeholder="prenom" name="prenom">
      <input type="text" placeholder="telephone" name="telephone">
      <input type="text" placeholder="ville" name="ville">
      <input type="text" placeholder="code postal" name="codepostal">
      <button type="submit">Accio</button>
  </form>

  <ul class="adresse">
  <% for(var i=0; i<adresse.length; i++) {%>
    <li class="client">
      <span><%= adresse[i].nom %></span>
      <span><%= adresse[i].prenom %></span>
      <span><%= adresse[i].telephone %></span>
      <span><%= adresse[i].ville %></span>
      <span><%= adresse[i].codepostal %></span>
      <form action="/modifier" method="GET"><input type="hidden" name="i" value="<%= i %>"><button type="submit">Modifier</button></form> 
    </li>
    <a href="detruire/<%= adresse[i]._id %>">détruire</a>

  <% } %>
  </ul>
  
  <script type="text/javascript" src="modifier_adresse.js"></script>

</body>
</html>
